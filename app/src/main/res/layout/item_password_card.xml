<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/spacing_md"
    android:background="@drawable/bg_glass_card"
    android:orientation="vertical"
    android:padding="@dimen/card_padding">

    <!-- Top Row: Category Icon + Security Badge + Favorite -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <!-- Favicon Container (New) -->
        <FrameLayout
            android:id="@+id/faviconContainer"
            android:layout_width="@dimen/category_icon_container"
            android:layout_height="@dimen/category_icon_container"
            android:background="@drawable/bg_input"
            android:layout_marginEnd="@dimen/spacing_sm">
            <ImageView
                android:id="@+id/ivFavicon"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_gravity="center"
                android:contentDescription="Website Icon"
                android:src="@drawable/ic_globe" />
        </FrameLayout>

        <!-- Category Icon Container -->
        <FrameLayout
            android:layout_width="@dimen/category_icon_container"
            android:layout_height="@dimen/category_icon_container"
            android:background="@drawable/bg_input">
            <ImageView
                android:id="@+id/ivCategoryIcon"
                android:layout_width="@dimen/icon_md"
                android:layout_height="@dimen/icon_md"
                android:layout_gravity="center"
                android:contentDescription="Category"
                tools:src="@drawable/ic_social" />
        </FrameLayout>

        <View
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <!-- Security Badge -->
        <TextView
            android:id="@+id/tvSecurityBadge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="@dimen/spacing_sm"
            style="@style/VaultX.Label"
            tools:text="HIGH SECURITY" />

        <!-- Favorite Button (New) -->
        <ImageView
            android:id="@+id/ivFavorite"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_star_outline"
            android:contentDescription="Toggle Favorite"
            android:background="?attr/selectableItemBackgroundBorderless" />
    </LinearLayout>

    <!-- Title -->
    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_md"
        style="@style/VaultX.Heading.Small"
        tools:text="GitHub Enterprise" />

    <!-- Username -->
    <TextView
        android:id="@+id/tvUsername"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_xxs"
        style="@style/VaultX.Body.Small"
        tools:text="alex_dev_master" />

    <!-- Masked Password + Copy -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_sm"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tvMaskedPassword"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="••••••"
            android:textColor="@color/vx_cyan"
            android:textSize="14sp"
            android:letterSpacing="0.15" />

        <ImageView
            android:id="@+id/ivCopy"
            android:layout_width="@dimen/icon_md"
            android:layout_height="@dimen/icon_md"
            android:src="@drawable/ic_copy"
            android:contentDescription="Copy password"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:padding="2dp" />
    </LinearLayout>

</LinearLayout>
